version: "3.7"

services:
  flask:
    build: ./src
    container_name: src
    restart: always
    environment:
      - APP_NAME=src
    expose:
      - 5000

  nginx:
    build: ./nginx
    container_name: nginx
    restart: always
    ports:
      - "80:80"
